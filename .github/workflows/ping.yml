name: Ping Monitor

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render App
        run: curl https://your-app-name.onrender.com/run-check?key=mysecret123
